# Modèle
model{
  
  # Vraisemblance
  for (i in 1:N){ 
    CD4t[i]~dnorm(mu[i],tau)
    mu[i] <- beta0 + beta1*LCV_50[i]
  }
  
  # A priori
  beta0~dnorm(0,0.0001) # propre mais très plat (faiblement informatif)
  beta1~dnorm(0,0.0001) # propre mais très plat (faiblement informatif)
  tau~dgamma(0.0001,0.0001) # propre mais très plat (faiblement informatif)
  
  # Variables d'interet
  sigma <- pow(tau, -0.5)
}